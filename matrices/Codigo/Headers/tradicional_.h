#ifndef TRADICIONAL_H
#define TRADICIONAL_H

#include <vector>

using namespace std;

typedef long long lld;  // Definir el tipo lld como long long int

// Declaración de la función de multiplicación de matrices tradicional
void multiply(const vector<vector<lld>>& A, const vector<vector<lld>>& B, vector<vector<lld>>& C);

#endif // TRADICIONAL_H

